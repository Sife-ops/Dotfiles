# THE FILE CONTAINING YOUR BITWARDEN SESSION KEY
# source this file in your shell's login profile

sessionKeyFile="${ZDOTDIR}/conf.d/99-bitwarden.zsh"

# THE COMMAND TO RUN WHEN EDITING OBJECTS
# default: xterm -e $EDITOR

editCmd="st -e $EDITOR"
